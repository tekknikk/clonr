[
{% for item in create_volume_log.results %}
  {
    "AvailabilityZone": "{{ item.AvailabilityZone }}",
    "Iops": "{{ item.Iops }}",
    "Size": "{{ item.Size }}",
    "SnapshotId": "{{ item.SnapshotId }}",
    "state": "{{ item.State }}",
    "VolumeId": "{{item.VolumeId}}",
    "InstanceId": "{{clones[loop.index0].id}}"
  }
{% if not loop.last -%}
,
{%- endif %}
{% endfor %}
]